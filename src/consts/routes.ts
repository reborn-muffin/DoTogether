enum ScreenRoutes {
  INDEX = '/',
  HOME = '/(app)/home',
  SIGN_IN = '/sign-in',
  SIGN_UP = '/sign-up',
  RESET_PASSWORD = '/reset-password',
  RESET_SUCCESSFUL = '/reset-successful',
}

enum ScreenNames {
  INDEX = 'index',
  SIGN_IN = 'sign-in',
  SIGN_UP = 'sign-up',
  RESET_PASSWORD = 'reset-password',
  RESET_SUCCESSFUL = 'reset-successful',
  APP_GROUP = '(app)',
  AUTH_GROUP = '(auth)',
}

export {ScreenRoutes, ScreenNames}
